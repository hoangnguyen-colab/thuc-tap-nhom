@{
    string cookieName = HttpContext.Current.User.Identity.Name;
}

<header>
    <div class="header-area transparent-bar ptb-55">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 col-md-4 col-4">
                    <div class="logo-small-device">
                        <a href="@Url.Action("Index", "Home")"><img alt="" src="/Assets/img/logo/logo.png"></a>
                    </div>
                </div>
                <div class="col-lg-8 col-md-8 col-8">
                    <div class="header-contact-menu-wrapper pl-45">
                        <div class="header-contact">
                            <p>WANT TO TALK WITH US  +01254 265 987</p>
                        </div>
                        <div class="menu-wrapper text-center">
                            <button class="menu-toggle">
                                <img class="s-open" alt="" src="~/Assets/img/icon-img/menu.png">
                                <img class="s-close" alt="" src="~/Assets/img/icon-img/menu-close.png">
                            </button>
                            <div class="main-menu">
                                <nav>
                                    <ul>
                                        <li><a href="@Url.Action("Index", "Home")">home</a></li>
                                        <li>
                                            <a href="@Url.Action("Shop", "Shop")">phone</a>
                                            <ul class="dropdown partial-content" data-url="@Url.Action("NavbarCategory", "Home")">
                                                <li><a>Loading</a></li>
                                            </ul>
                                        </li>
                                        <li class="active">
                                            <a href="@Url.Action("About", "Home")">about</a>
                                        </li>
                                        <li><a href="@Url.Action("Contact", "Home")">contact</a></li>
                                        @if (!string.IsNullOrEmpty(cookieName))
                                        {
                                            <li>
                                                <a href="javascript:void(0);">
                                                    Hello @cookieName
                                                </a>
                                                <ul class="dropdown">
                                                    <li>
                                                        <a href="@Url.Action("CustomerProfile", "Customer", new { username = cookieName })">
                                                            profile
                                                        </a>
                                                    </li>
                                                    <li><a href="@Url.Action("Logout", "Customer")">logout</a></li>
                                                </ul>
                                            </li>
                                        }
                                        else
                                        {
                                            <li>
                                                <a href="@Url.Action("Login", "Customer")">login</a>
                                            </li>
                                        }
                                    </ul>
                                </nav>
                            </div>
                        </div>
                    </div>
                    <div class="header-cart cart-small-device cart-partial">
                    </div>
                </div>
                <!--Mobile-->
                <div class="mobile-menu-area col-12">
                    <div class="mobile-menu">
                        <nav id="mobile-menu-active">
                            <ul class="menu-overflow">
                                <li><a href="@Url.Action("Index", "Home")">home</a></li>
                                <li>
                                    <a href="@Url.Action("Shop", "Shop")">phone</a>
                                    <ul class="dropdown partial-content" data-url="@Url.Action("NavbarCategory", "Home")">
                                        <li><a>Loading</a></li>
                                    </ul>
                                </li>
                                <li class="active">
                                    <a href="@Url.Action("About", "Home")">about</a>
                                </li>
                                <li><a href="@Url.Action("Contact", "Home")">contact</a></li>
                                <li>
                                    <a href="@Url.Action("Cart", "Cart")">cart</a>
                                </li>
                                @if (!string.IsNullOrEmpty(cookieName))
                                {
                                    <li>
                                        <a>Hello @cookieName</a>
                                        <ul class="dropdown">
                                            <li>
                                                <a href="@Url.Action("CustomerProfile", "Customer", new { username = cookieName })">
                                                    profile
                                                </a>
                                            </li>
                                            <li><a href="@Url.Action("Logout", "Customer")">logout</a></li>
                                        </ul>
                                    </li>
                                }
                                else
                                {
                                    <li>
                                        <a href="@Url.Action("Login", "Customer")">login</a>
                                    </li>
                                }
                            </ul>
                        </nav>
                    </div>
                </div>
            </div>
        </div>
        <div class="header-cart-wrapper">
            <div class="header-cart cart-partial">
            </div>
        </div>
    </div>
</header>

<script>
    $(document).ready(function () {
        $.get("@Url.Action("CartPartial", "Cart")",
            function (data) {
                $(".cart-partial").html(data);
                $(".icon-cart").on("click", function () {
                    $(this).parent().find('.shopping-cart-content').slideToggle('medium');
                });
            }
        );
    });
</script>

